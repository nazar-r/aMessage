import { RenderingButtons } from "../tsx.items/items.login.buttons";

const LoginPageContent = () => (
    <div className="login-page">
        <div className="login-page__title">Join or sign in</div>
        <RenderingButtons />
    </div>
);

export default LoginPageContent;